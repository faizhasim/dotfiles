### Nix template
# Ignore build outputs from performing a nix-build or `nix build` command
result
result-*

# Ignore idea files
.idea/
*.iml
*.iws

### Private configuration files
# gh-dash private repos (unencrypted, local only)
# The encrypted version (*.age) is committed in secrets/
home-manager/gh-dash/repos.toml
